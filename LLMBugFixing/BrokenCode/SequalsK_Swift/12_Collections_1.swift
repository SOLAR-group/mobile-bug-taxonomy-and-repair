func inCollection(items: Array<String>) -> String {
    var str = "Nothing to say."
    if "orange" items {
        str = "juicy"
    } else if "apple" items {
        str = "apple is fine too"
    }

    return str
}
