// Conditional expressions

func conditional(a: Int, b: Int) -> Int {
    if (a > b) {
        return a
    } else {
        return b
    }
}

func conditionalAsExpression(a: Int, b: Int) -> Int {
    if (a > b) {
        return a
    } else {
        return b
    }
}